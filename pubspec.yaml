name: enhanced_chameleon_ultra_gui
description: Enhanced cross-platform GUI for Chameleon Ultra with Proxmark3 Iceman firmware compatibility.
publish_to: "none"

version: 2.0.0+0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true